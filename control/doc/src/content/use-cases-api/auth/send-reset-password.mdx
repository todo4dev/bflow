---
title: Send Reset Password
---

# Send Reset Password

Initiates the password recovery process by sending an email with a reset code.

## Description

This endpoint allows a user to request a password reset. If the account exists, an email with a One-Time Password (OTP) is sent to the provided address using the "recover.html" template.

## Rules

- The endpoint must be accessed via a `POST` request.
- **Email**: Must be a valid email format.
- If the email is not found, a `404 Not Found` is returned.
- Upon success, a recovery email is sent with a generated OTP.

## Request

- **Method:** `POST`
- **Path:** `/auth/recover`
- **Body:** `application/json`

| Field | Type   | Required | Description               |
|-------|--------|----------|---------------------------|
| email | string | Yes      | The user's email address. |

### Example

```json
{
  "email": "john.doe@email.com"
}
```

## Success Case

**Condition:** valid email, account exists.

- **HTTP Status:** `202 Accepted`
- **Response Body:** Empty

## Error Cases

### Account Not Found

**Condition:** The provided email is not registered.

- **HTTP Status:** `404 Not Found`
- **Response Body:**
  ```json
  {
    "name": "AccountNotFound",
    "message": "Account not found"
  }
  ```

### Invalid Data

**Condition:** Email format invalid or missing fields.

- **HTTP Status:** `422 Unprocessable Entity`
- **Response Body:** Validation error details.
